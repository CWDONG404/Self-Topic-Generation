# 项目协作约定

## Git 提交

- 每次完成代码、配置或文档改动并通过相应验证后，默认执行 `git add`、`git commit` 和 `git push`，无需用户再次提醒。
- 提交前检查变更范围与敏感信息；不得提交 `.env`、API Key、密码、令牌或本地运行数据。
- 提交信息使用简洁、可识别的中文说明；推送到当前分支及其已配置的远程上游，除非用户另有指定。
- 如果提交或推送失败，应明确说明原因并继续排查，不得声称已经同步到 GitHub。
